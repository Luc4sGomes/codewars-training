function checkCoupon(enteredCode, correctCode, currentDate, expirationDate) {

  let keepCurrentDateFormated = Number(currentDate.substring(currentDate.length-1, currentDate.length));

  console.log(keepCurrentDateFormated);
}

checkCoupon('123','123','September 5, 2014','October 1, 2014');

let x = function() {
 
}